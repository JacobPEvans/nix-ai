# README validation config for nix-ai
# This is a Nix home-manager module repo — READMEs are module documentation,
# not user-facing library READMEs. Sections are tailored accordingly.
required_sections:
  - What it manages
optional_sections:
  - Transports
  - Adding New Servers
  - Enabling / Disabling Servers
  - Secrets Management
  - Troubleshooting
